@{
    Layout = "_Layout1";
    ViewData["Title"] = "Services by Price";
}
@model List<Service>

<section class="app-section">
    <div class="container">
        <div class="app-page-header d-flex flex-column flex-lg-row justify-content-between gap-3">
            <div>
                <span class="eyebrow">Sorted by price</span>
                <h1 class="fw-bold mt-2 mb-2">Affordable services first</h1>
                <p class="lead-muted mb-0">Compare service listings from lowest price upward.</p>
            </div>
            <div class="align-self-lg-end">
                <a href="/Services/Index" class="btn btn-outline-primary">Back to marketplace</a>
            </div>
        </div>

        <div class="row g-4">
            @if (Model != null && Model.Any())
            {
                @foreach (var service in Model)
                {
                    <div class="col-sm-6 col-lg-4">
                        <div class="card service-card">
                            <img src="/@service.referenceImagePath" class="card-img-top" alt="@service.title">
                            <div class="card-body">
                                <h5 class="card-title fw-bold">@service.title</h5>
                                <p class="lead-muted mb-2">Provider: @service.provider</p>
                                <p class="fw-bold mb-3">Starting price: @service.pricing</p>
                                <a href="/Services/ServiceDetails/@service.id" class="btn btn-primary w-100">View details</a>
                            </div>
                        </div>
                    </div>
                }
            }
            else
            {
                <div class="col-12"><div class="empty-state">No services available yet.</div></div>
            }
        </div>
    </div>
</section>
